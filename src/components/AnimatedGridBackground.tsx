import React from 'react';

const AnimatedGridBackground: React.FC = () => {
  return (
    <div className="animated-grid-background absolute inset-0 -z-10 h-full w-full">
      {/* The background grid and animated spotlight effect are handled entirely by CSS */}
    </div>
  );
};

export default AnimatedGridBackground;
